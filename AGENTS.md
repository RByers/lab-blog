# AGENTS.md

## Repository

The canonical remote is https://github.com/RByers/lab-blog (`origin`).

## Pushing work

Once the user has confirmed the change, push it directly to `origin` — no pull
request or fork is needed. Always get that confirmation from the user first;
never push unreviewed work.
